import { SearchBox } from "../components/SearchBox";

export default { component: SearchBox };

export const SearchBoxStory = { args: {} };

export const SearchBoxStoryWithCss = {
    args: {
        cssExtra: "border: 1px solid red;",
    },
};
